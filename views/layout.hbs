<!DOCTYPE html>
<html>
  <head>
    <title>{{title}}</title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <header class="header">
    <p class="postTitle">Post Your Vinyl Collection</p>
    <nav class="nav">
      <a href="/auth/instagram">Login</a>
      <a href="/logout">Logout</a>
    </nav>
  </header>
  <body>
  <p>
    {{#if user}}
      Hello {{user.username}}!
      {{else}}
      Not Logged In{{user.displayName}}!
        {{/if}}
    {{{body}}}
  </p>
  </body>
</html>
